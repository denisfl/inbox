# Production overrides for docker-compose.yml
# Usage: docker compose -f docker-compose.yml -f docker-compose.production.yml up -d

services:
  web:
    build:
      target: production
      args:
        GIT_SHA: ${GIT_SHA:-}
    command: bundle exec rails server -b 0.0.0.0
    # Do NOT mount source code in production — image is self-contained
    volumes:
      - storage_data:/app/storage
      - log_data:/app/log
    environment:
      - RAILS_ENV=production
      - DATABASE_URL=sqlite3:/app/storage/production.sqlite3
      - REDIS_URL=redis://redis:6379/0
      - RAILS_LOG_TO_STDOUT=true
      # Secrets loaded from /run/secrets/* by bin/docker-entrypoint
      # TELEGRAM_BOT_TOKEN and TELEGRAM_WEBHOOK_SECRET_TOKEN are injected automatically
      - TELEGRAM_BOT_NAME=${TELEGRAM_BOT_NAME}
      - TELEGRAM_ALLOWED_USER_ID=${TELEGRAM_ALLOWED_USER_ID}
      - TELEGRAM_WEBHOOK_URL=${TELEGRAM_WEBHOOK_URL}
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - WEB_PASSWORD=${WEB_PASSWORD}
      - API_TOKEN=${API_TOKEN}
      - TRANSCRIBER_URL=http://transcriber:5000
      - RAILS_HOSTS=${RAILS_HOSTS}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - GOOGLE_REFRESH_TOKEN=${GOOGLE_REFRESH_TOKEN}
      - GOOGLE_CALENDAR_IDS=${GOOGLE_CALENDAR_IDS:-primary}
      - CALENDAR_REMINDER_MINUTES=${CALENDAR_REMINDER_MINUTES:-10}
      # Omit TRANSCRIBER_LANGUAGE to use auto-detection; set to 'ru' or 'en' to force a language
      # - TRANSCRIBER_LANGUAGE=ru
    ports:
      - "3000:3000"
    secrets:
      - telegram_bot_token
      - telegram_webhook_secret_token

  worker:
    build:
      target: production
      args:
        GIT_SHA: ${GIT_SHA:-}
    command: bundle exec rails solid_queue:start
    volumes:
      - storage_data:/app/storage
      - log_data:/app/log
    environment:
      - RAILS_ENV=production
      - DATABASE_URL=sqlite3:/app/storage/production.sqlite3
      - REDIS_URL=redis://redis:6379/0
      - RAILS_LOG_TO_STDOUT=true
      - TELEGRAM_ALLOWED_USER_ID=${TELEGRAM_ALLOWED_USER_ID}
      - SECRET_KEY_BASE=${SECRET_KEY_BASE}
      - TRANSCRIBER_URL=http://transcriber:5000
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - RAILS_HOSTS=${RAILS_HOSTS}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - GOOGLE_REFRESH_TOKEN=${GOOGLE_REFRESH_TOKEN}
      - GOOGLE_CALENDAR_IDS=${GOOGLE_CALENDAR_IDS:-primary}
      - CALENDAR_REMINDER_MINUTES=${CALENDAR_REMINDER_MINUTES:-10}
      # Omit TRANSCRIBER_LANGUAGE to use auto-detection; set to 'ru' or 'en' to force a language
      # - TRANSCRIBER_LANGUAGE=ru
    secrets:
      - telegram_bot_token
      - telegram_webhook_secret_token

  # Close internal ports in production
  redis:
    ports: []

  transcriber:
    ports: []

secrets:
  telegram_bot_token:
    file: ./secrets/telegram_bot_token
  telegram_webhook_secret_token:
    file: ./secrets/telegram_webhook_secret_token
