<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Inbox - Block Notes" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="Inbox">
    <meta name="application-name" content="Inbox">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="Capture and organize your thoughts from Telegram">
    <meta name="theme-color" content="#2563eb">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# PWA manifest for installable app %>
    <link rel="manifest" href="/manifest.json">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

    <body class="antialiased" data-controller="pwa">
    <div class="app-container">
      <main class="main-content">
        <%= render "layouts/header" %>
        
        <div class="content-wrapper">
          <%= yield %>
        </div>
      </main>
      
      <%# PWA Install Button (shown on mobile when installable) %>
      <div class="pwa-install-prompt hidden" data-pwa-target="installButton">
        <div class="pwa-install-card">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M12 5v14m0 0l7-7m-7 7l-7-7"/>
          </svg>
          <div class="pwa-install-content">
            <h3>Install Inbox</h3>
            <p>Install this app on your device for quick access and offline use</p>
          </div>
          <button class="btn btn-primary" data-action="click->pwa#install">Install</button>
          <button class="btn btn-text" data-action="click->pwa#hideInstallButton">Not now</button>
        </div>
      </div>
      
      <%# PWA Update Button (shown when new version available) %>
      <div class="pwa-update-banner hidden" data-pwa-target="updateButton">
        <span>New version available!</span>
        <button class="btn btn-sm" data-action="click->pwa#update">Update</button>
      </div>
    </div>
  </body>
</html>
