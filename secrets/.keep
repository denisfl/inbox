# This directory holds Docker secrets files (bot token, webhook secret, etc.)

# It is excluded from git via .gitignore — NEVER commit actual secret files here.

# See .project/deployment-quickstart.md Step 4 for setup instructions.
